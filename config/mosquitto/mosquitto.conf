# Mosquitto config for SquareGuardian
# Anonymous access within Docker network (no auth needed for internal services)
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
