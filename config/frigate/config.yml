# Fallback config — overwritten on container start by generate-frigate-config.sh.
# This file exists so Frigate can boot even if the entrypoint script is skipped.

mqtt:
  enabled: false

tls:
  enabled: false

auth:
  enabled: false

birdseye:
  enabled: true

record:
  enabled: false

detectors:
  cpu1:
    type: cpu

cameras: {}
